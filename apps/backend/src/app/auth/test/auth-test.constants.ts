export enum AuthTestDescription {
  Controller = 'AuthController',
  Login = 'login should return tokens',
  RefreshTokens = 'refreshTokens should return new tokens',
  Register = 'register should return new user'
}
